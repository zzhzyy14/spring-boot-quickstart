server:
  port: ${order-service.port:8020}

spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:7001
        username: nacos
        password: nacos